openapi: 3.0.3
info:
  title: HardShell Core API
  version: 0.1.0
servers:
  - url: https://api.example.com/v1

paths:
  /healthz:
    get:
      summary: Health check
      responses:
        "200":
          description: OK

components:
  securitySchemes:
    firebaseIdToken:
      type: http
      scheme: bearer
      bearerFormat: JWT